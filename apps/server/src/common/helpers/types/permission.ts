export enum UserRole {
  OWNER = 'owner',
  ADMIN = 'admin', 
  MEMBER = 'member',
}

export enum SpaceRole {
  ADMIN = 'admin', 
  WRITER = 'writer', 
  READER = 'reader', 
}

export enum SpaceVisibility {
  OPEN = 'open', 
  PRIVATE = 'private', 
}
